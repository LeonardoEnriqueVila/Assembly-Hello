section .text
    global _start

_start:
    mov eax, 5    ; Carga el número 5 en el registro EAX
    add eax, 3    ; Suma 3 al valor de EAX (5 + 3)
    ; Ahora, EAX contiene 8
    
    ; En este punto, deberías insertar las instrucciones necesarias para
    ; terminar correctamente el programa en Windows. Sin embargo, esto es
    ; solo un ejemplo de operación aritmética y el programa no está diseñado
    ; para ser ejecutado en un entorno Windows real sin modificaciones adicionales.
